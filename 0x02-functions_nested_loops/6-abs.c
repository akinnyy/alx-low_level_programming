#include "main.h"
/**
 * _abs - main function
 *
 * @n: integer to make absolute with the program
 *
 * Return: int or negative int
 *
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (n * -1);
}

