#include "main.h"

/**
 * _strlen - returns the length of any string
 *
 * @s:The string whose length is required
 *
 * Return:0
 */
int _strlen(char *s)
{
	int len;

	len = strlen(*s);
	return (len)
}
